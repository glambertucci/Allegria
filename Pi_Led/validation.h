#ifndef _validation_
#define _validation_

#define ON (1)
#define OFF (0)
#define TRUE (1)
#define FALSE (0)

int op_valid (char input);
int check_mask (char * mask_array, int max_bits);
int bit_validation (char bit);

#endif
